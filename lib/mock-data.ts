/**
 * Deprecated mock layer.
 *
 * The app now uses:
 * - Google Sheets / Apps Script weekly status for coaching state
 * - local deterministic exercise libraries in /data/exercises
 * - buildWorkoutFromStatus() in /lib/adapters/build-workout-from-status
 *
 * Keep this file only as a historical marker so future work does not rebuild the app around mock data.
 */
export {}
